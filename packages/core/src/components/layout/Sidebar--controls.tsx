import React from 'react';
import styled from 'styled-components';

const Style = styled.div`
  /* padding: ${props => props.theme.padding.md}; */
`;

const SidebarControls = () => {
  return <Style>hi</Style>;
};

export default SidebarControls;
